# `function functionName[typeParameters restrictionsToTypeParameters](arguments) returnedType {…}` — type parameters —
* Check '../moreTypes/functions'
* allows
  * functions can work with multiple types
* `restrictionsToTypeParameters`
  * Example
    * `comparable`
      * → allows using
        * `==`
        * `!=`
* `go run index.go`