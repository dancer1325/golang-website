# Go & Java -- Common aspects --
* Object oriented
  * but Go does NOT have the keywords -- `class` OR `extends` OR `implements` --
* Garbage collected
* Statically typed
* Part of C family

# All types are created equal
* TODO: